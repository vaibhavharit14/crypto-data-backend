services:
  - type: web
    name: mcp-server
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python app/utils/main.py"
    envVars:
      - key: API_KEY
        value: dummy_key